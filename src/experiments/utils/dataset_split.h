#ifndef KAPPA_DATASET_SPLIT_H
#define KAPPA_DATASET_SPLIT_H

unsigned long number_of_lines(std::string);
std::vector<unsigned long> dataset_split_by_size(unsigned long, unsigned long, unsigned long);

#endif //KAPPA_DATASET_SPLIT_H
